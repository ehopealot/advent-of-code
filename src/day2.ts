import {loadPuzzleInput} from './lib/load_file'

export function part1(example=false) {
  const inp = loadPuzzleInput("2", "1", example);
  return 0;
}

export function part2(example=false) {
  const inp = loadPuzzleInput("2", "2", example);
  return 0;
}
